1. 더 작은 값을 가질 수 있다.
2. 항상 작은 값을 가지지 않는다.
3. n/2개이다.
4. 최악의 경우:  Theta(log n)  최선의 경우: Theta(1)
5. 간단하다. 힙은 완전 이진 트리이기 때문이다.
6. 비효율적이다. 기존 방식은 O(n), 학생의 방식은 O(n log n)이다. 기존 방식은 높이가 낮은 노드는 조금 움직이고, 높이가 높은 노드가 많이 움직여서 선형 시간에 가능하다. 그러나 학생의 방식은 원소를 하나씩 삽입하며 힙을 키워나가는 것과 같다. 이는 깊이가 깊은 리프 쪽 노드들이 최악의 경우 트리 높이만큼 계속 올라와야 하므로 더 느리다.
7. 스며오르기 과정을 수행해야 한다. 해당 노드의 값을 증가된 값으로 바꿈 -> 해당 노드의 값과 부모 노드의 값을 비교 -> 만약 해당 노드의 값이 부모보다 크다면 두 위치를 바꿈 -> 부모가 더 크거나 루트 노드에 도달할 때까지 2~3번 과정을 반복.
